# CodeAlpha_Basic_Network_Sniffer
This repository contains a Python program built to capture network traffic packets. It utilizes the scapy library for packet capturing. The tool successfully extracts and displays useful information such as source and destination IPs, network protocols (TCP, UDP, ICMP), and raw payloads.
